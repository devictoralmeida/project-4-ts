export type TPokemonStatus = {
    base_stat: number
    stat: 'hp' | 'attack' | 'defense'
}

export type TCountryName = {
    common: string;
    official: string;
}

export type TFlag = {
    png: string;
    svg: string;
}